name: sphmoments

# Triggers on push or pull request to main when this formula or workflow changes
on:
  push:
    paths:
    - 'sphmoments.rb'
    - '.github/workflows/sphmoments.yml'
    branches: [ main ]
  pull_request:
    paths:
    - 'sphmoments.rb'
    - '.github/workflows/sphmoments.yml'
    branches: [ main ]

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  install:
    # The type of runner that the job will run on
    runs-on: macos-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
    - uses: actions/checkout@v5

    - name: tap the repo
      run: |
        brew tap danieljprice/all
        brew trust danieljprice/all

    - name: install sphmoments with homebrew
      run: brew install sphmoments
